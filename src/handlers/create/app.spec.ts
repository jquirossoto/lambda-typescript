import { v4 as uuid } from 'uuid';

describe('Create', () => {
    describe('Handler', () => {
        it('Should return 200 with a created book.', () => {
            expect(true).toBe(true);
        });
    });
});
